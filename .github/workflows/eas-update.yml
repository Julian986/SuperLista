name: EAS Update

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'superlista/app/**'
      - 'superlista/components/**'
      - 'superlista/contexts/**'
      - 'superlista/hooks/**'
      - 'superlista/lib/**'
      - 'superlista/utils/**'
      - 'superlista/types/**'
      - 'superlista/constants/**'
      - 'superlista/config/**'

jobs:
  update:
    name: Publicar EAS Update
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🏗️ Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: npm
          cache-dependency-path: superlista/package-lock.json

      - name: 🏗️ Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Instalar dependencias
        working-directory: superlista
        run: npm ci

      - name: 🚀 Publicar update a canal production
        working-directory: superlista
        run: |
          eas update --channel production --non-interactive --message "CI production update $GITHUB_SHA"

      - name: ✅ Actualización completada
        run: echo "✅ Update publicado exitosamente al canal production"


